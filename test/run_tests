#! /bin/sh
if [ -z "$*" ]; then
    python ../scapy/tools/UTscapy.py -t regression.uts -f html -l -o /tmp/scapy_regression_test_$(date +%Y%m%d-%H%M%S).html
else
    python ../scapy/tools/UTscapy.py "$@"
fi
